[defaults]
inventory = inventory.ini
remote_user = root
private_key_file = ~/.ssh/paysaxas
host_key_checking = False
pipelining = True
stdout_callback = yaml
timeout = 30
gathering = smart
fact_caching = memory

[privilege_escalation]
become = True
become_method = sudo
become_user = root
